{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Editor Sidecar (Docker)",
      "type": "node",
      "request": "attach",
      "address": "localhost",
      "port": 9229,
      "localRoot": "${workspaceFolder}/editor-container",
      "remoteRoot": "/app",
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}